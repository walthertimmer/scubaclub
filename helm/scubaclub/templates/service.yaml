apiVersion: v1
kind: Service
metadata:
  name: {{ include "scubaclub.fullname" . }}
  labels:
    {{- include "scubaclub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scubaclub.selectorLabels" . | nindent 4 }}
