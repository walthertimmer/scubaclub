apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scubaclub.fullname" . }}-test-connection"
  labels:
    {{- include "scubaclub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scubaclub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
